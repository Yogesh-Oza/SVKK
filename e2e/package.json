{
  "name": "svkk-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright e2e tests for SVKK (live / local via e2e/.env)",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:age25": "npx playwright test tests/age25-carry-forward.spec.ts --headed",
    "test:login": "npx playwright test tests/login.spec.ts",
    "test:university": "npx playwright test tests/university.spec.ts",
    "test:registration": "npx playwright test tests/student-registration.spec.ts",
    "test:debug": "npx playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
