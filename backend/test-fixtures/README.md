# Policy CSV API test fixture

`policy-import-api-test.csv` is generated by the API integration script (unique SVKK ID / policy no per run).

```bash
cd backend
npx tsx scripts/test-policy-csv-upload.ts
```

Optional env: `API_BASE`, `TEST_EMAIL`, `TEST_PASSWORD`, `CSV_OUT`.
